{
  "name": "@netcheckup/landing",
  "version": "0.1.0",
  "private": true,
  "description": "Landing page de NetCheckup",
  "scripts": {
    "dev": "npx serve . -p 3000 -s",
    "build": "echo 'Static page — no build needed'"
  }
}
